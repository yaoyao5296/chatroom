# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/tasks.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vite

# Trae IDE workspace rules（不要忽略，项目运行说明）
!.trae/rules/project_rules.md

# pnpm store
.pnpm-store/

# Environment variables (contains email/SMS credentials)
.env
*.keystore

# Database
data/
*.db
*.db-shm
*.db-wal

# Uploads
uploads/

# JWT secret (auto-generated, do not commit)
.jwt_secret

# Electron build output
release/

# ========== 本次新增忽略（推送安全 / 构建体积优化）==========
# GitHub 2FA / 账号凭据类文件（私钥不能推）
GITHUB_2FA_BACKUP.txt
*_2FA_BACKUP*
*_BACKUP.txt

# Redis 持久化 RDB / AOF
dump.rdb
*.rdb
appendonly.aof

# Vite 前端构建产物 dist/（运行时由 npm build 生成，不进仓库）
dist/
dist-ssr/
electron/dist/

# 临时日志 / 自动启动运行输出
/tmp/
/tmp/autostart.log

# 本地 SSH 临时文件
codespaces.auto*

# APK/签名构建产物
android/app/build/
android/app/release/
android/app/debug/
*.apk
*.aab
*.keystore
*.jks

# Node modules
node_modules/

# iOS build artifacts
ios/node_modules
keystore.properties
dist/
*.bak
.url-*.txt
cf-*.sh
